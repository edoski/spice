defaults:
  - chain: ethereum
  - dataset: default
  - model: lstm
  - provider: publicnode
  - acquisition: default
  - split: default
  - training: default
  - simulation: default
  - tracking: mlflow
  - tuning: default
  - runtime: local
  - _self_

hydra:
  job:
    chdir: false
  run:
    dir: ${runtime.hydra_run_dir}
  sweep:
    dir: ${runtime.hydra_sweep_dir}
