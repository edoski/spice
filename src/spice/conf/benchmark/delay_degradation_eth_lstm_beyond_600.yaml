cases:
  - id: delay_degradation_eth_lstm_beyond_600
    base:
      surface: current_row_fee_dynamics
      training: default
      split: default
      study: delay_degradation_eth_lstm_beyond_600
      evaluation_window:
        start: "2026-02-03T14:00:00Z"
        duration_seconds: 7200
    dimensions:
      data:
        - set: {chain: ethereum, corpus_id: cor_9a73b1e88edb488afb1e}
      models:
        - set:
            model: lstm
            tuning_space: lstm_large_capacity
      problems:
        - grid:
            base: current_row_nominal
            fields:
              max_delay_seconds: [720, 840, 960, 1080, 1200]
    steps:
      - id: train_baseline
        workflow: train
        after:
          - slurm: afterok:59648
        set:
          variant: baseline
      - id: evaluate_baseline
        workflow: evaluate
        after: [train_baseline]
        artifact_from: train_baseline
        set:
          evaluator: poisson_replay
