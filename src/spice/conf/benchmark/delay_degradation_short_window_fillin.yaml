cases:
  - id: delay_degradation_short_window_fillin
    base:
      surface: current_row_fee_dynamics
      training: default
      split: default
      study: delay_degradation_short_window_fillin
      evaluation_window:
        start: "2026-02-03T14:00:00Z"
        duration_seconds: 7200
    dimensions:
      data:
        - set: {chain: polygon, corpus_id: cor_415186063685f5b7588f}
        - set: {chain: avalanche, corpus_id: cor_91fa9b80b6014317c148}
      models:
        - set:
            model: lstm
            tuning_space: lstm_large_capacity
      problems:
        - grid:
            base: current_row_nominal
            fields:
              max_delay_seconds: [2, 4, 6, 8, 10]
    steps:
      - id: train_baseline
        workflow: train
        after:
          - slurm: afterok:59271
        set:
          objective: profit_poisson_replay
          evaluator: poisson_replay
          variant: baseline
      - id: evaluate_baseline
        workflow: evaluate
        after: [train_baseline]
        artifact_from: train_baseline
        set:
          evaluator: poisson_replay
