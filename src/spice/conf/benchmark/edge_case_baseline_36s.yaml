cases:
  - id: ethereum_edge_case_baseline_36s
    base:
      surface: current_row_fee_dynamics
      chain: ethereum
      corpus_id: cor_2edb8f7b84a4edf95e2b
      training: default
      split: default
      study: ethereum_edge_case_baseline_36s
      evaluations: ethereum_pectra_edge_cases
    dimensions:
      models:
        - set:
            model: lstm
            tuning_space: lstm_large_capacity
        - set:
            model: transformer
            tuning_space: transformer_large_capacity
        - set:
            model: transformer_lstm
            tuning_space: transformer_lstm_large_capacity
      problems:
        - ref: current_row_nominal
    steps:
      - id: train_baseline
        workflow: train
        set:
          objective: profit_poisson_replay
          evaluator: poisson_replay
          variant: baseline
      - id: evaluate_baseline
        workflow: evaluate
        after: [train_baseline]
        artifact_from: train_baseline
        set:
          evaluator: poisson_replay

  - id: polygon_edge_case_baseline_36s
    base:
      surface: current_row_fee_dynamics
      chain: polygon
      corpus_id: cor_61fb33e47c948a9cebd0
      training: default
      split: default
      study: polygon_edge_case_baseline_36s
      evaluations: polygon_bhilai_edge_cases
    dimensions:
      models:
        - set:
            model: lstm
            tuning_space: lstm_large_capacity
        - set:
            model: transformer
            tuning_space: transformer_large_capacity
        - set:
            model: transformer_lstm
            tuning_space: transformer_lstm_large_capacity
      problems:
        - ref: current_row_nominal
    steps:
      - id: train_baseline
        workflow: train
        set:
          objective: profit_poisson_replay
          evaluator: poisson_replay
          variant: baseline
      - id: evaluate_baseline
        workflow: evaluate
        after: [train_baseline]
        artifact_from: train_baseline
        set:
          evaluator: poisson_replay

  - id: avalanche_edge_case_baseline_36s
    base:
      surface: current_row_fee_dynamics
      chain: avalanche
      corpus_id: cor_3ef359c91addcab77e9f
      training: default
      split: default
      study: avalanche_edge_case_baseline_36s
      evaluations: avalanche_octane_edge_cases
    dimensions:
      models:
        - set:
            model: lstm
            tuning_space: lstm_large_capacity
        - set:
            model: transformer
            tuning_space: transformer_large_capacity
        - set:
            model: transformer_lstm
            tuning_space: transformer_lstm_large_capacity
      problems:
        - ref: current_row_nominal
    steps:
      - id: train_baseline
        workflow: train
        set:
          objective: profit_poisson_replay
          evaluator: poisson_replay
          variant: baseline
      - id: evaluate_baseline
        workflow: evaluate
        after: [train_baseline]
        artifact_from: train_baseline
        set:
          evaluator: poisson_replay
