cases:
  - id: slot_spacing_sweep
    base:
      surface: current_row_fee_dynamics
      training: default
      split: default
      study: slot_spacing_sweep
    dimensions:
      data:
        - set: {chain: ethereum}
        - set: {chain: polygon}
        - set: {chain: avalanche}
      models:
        - set:
            model: lstm
            tuning_space: lstm_large_capacity
        - set:
            model: transformer
            tuning_space: transformer_large_capacity
        - set:
            model: transformer_lstm
            tuning_space: transformer_lstm_large_capacity
      problems:
        - ref: current_row_nominal
        - ref: current_row_recent_median
    steps:
      - id: train_baseline
        workflow: train
        set:
          objective: profit_poisson_replay_2h
          evaluation: poisson_replay_2h
          variant: baseline
      - id: evaluate_baseline
        workflow: evaluate
        after: [train_baseline]
        set:
          objective: profit_poisson_replay_2h
          evaluation: poisson_replay_2h
          variant: baseline
