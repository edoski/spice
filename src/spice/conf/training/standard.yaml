learning_rate: 0.0003
weight_decay: 0.01
batch_size: 512
max_epochs: 50
early_stopping:
  patience: 8
  min_delta: 0.0001
gradient_clip_norm: 1.0
seed: 2026
deterministic: true
log_every_n_steps: 100
input_normalization:
  id: row_standard
